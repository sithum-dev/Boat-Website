import BootVideo from "./BootVideo";
import HeroText from "./HeroText";
import MapImages from "./MapImages";

const Home = () => {
  return (
    <div>
      <HeroText />
      <MapImages />
      <BootVideo />
    </div>
  );
};

export default Home;
